public class play {
    public static void main(String[] args) {
        // s = s_0 + v_0 * t + 1/2 * g * Math.pow(t, 2);
        // FV = PV * Math.pow(1 + INT / 100, YRS);
        // G = 4 * Math.pow(Math.PI, 2) * Math.pow(a, 3) / (Math.pow(p, 2) * (m_1 + m_2));
        // c = Math.pow(Math.pow(a, 2) + Math.pow(b, 2) - 2 * a * b * Math.cos(y), 1/2);

        System.out.println(Character);
        for (int i = 0; i < args.length; i++) {
            
        }
        for (String string : args) {
            
        }
        while (condition) {
            
        }
        do {
            
        } while (condition);
    }
}
